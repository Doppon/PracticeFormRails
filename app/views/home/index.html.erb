<div style="text-align: center">
  <h2>form_tagの練習</h2>
  <%= form_tag index_path do |f| %>
    <div> 
      <p>テキストエリア</p>
      <%= text_area_tag :message %>
    </div>
    <div>
      <%= submit_tag '更新！' %>
    </div>
  <% end %>
  <p>※注目((*｀艸´)ｳｼｼｼ)→<%= @data %></p>
</div>
